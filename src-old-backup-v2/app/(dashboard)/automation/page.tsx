import { ModulePlaceholder } from "@/components/shared/module-placeholder";

export default function Page() {
  return (
    <ModulePlaceholder
      title="Automation"
      description="Workflow rules, scheduled jobs, and event triggers."
      phase="a later phase"
    />
  );
}
